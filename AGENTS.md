# Repository Instructions

- Keep code simple and readable.
- Preserve metadata through every ingestion and retrieval stage.
- Use retrieved context only when answering.
- Use ChromaDB only for v1 vector storage.
- Add logging and basic exception handling in each module.
- Ensure README commands match implementation.
- If context is insufficient, return: "I could not find this in the indexed Databricks docs."
